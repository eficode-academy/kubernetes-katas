apiVersion: v1
kind: Pod
metadata:
  labels:
    run: frontend
  name: frontend
spec:
  containers:
  - image: ghcr.io/eficode-academy/flask-quotes-frontend:release
    name: frontend
    resources: {}
  restartPolicy: Always
